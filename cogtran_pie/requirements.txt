# Local helper scripts (train/evaluate) depend on these libraries.
# We avoid the official CognateTransformer requirements file because it pins
# lingpy==2.6.9, which is incompatible with modern pip metadata validation.
numpy
pandas
datasets>=2.13,<5
transformers>=4.24,<5
accelerate>=0.26.0
torch
fair-esm==2.0.0
lingpy>=2.6.13
